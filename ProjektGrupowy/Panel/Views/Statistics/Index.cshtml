@model Panel.Models.StatistiscViewModel
@{
    ViewBag.Title = "Statistics";
}

<body>
    <h2>Statistics</h2>

</body>

<p>Statistics of user @User.Identity.Name</p>

<div class="row">
    @*@using (Html.BeginForm("Add", "EventConfiguration", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
    {*@
        <div class="form-group">
            <label for="type-input">Chart type:</label>
            @*@Html.DropDownListFor(x => x.CurrentChartType, new SelectList(Model.ChartTypes, "Id", "Name"))*@
        </div>
        <div class="form-group">
            <label for="period-input">Show in period:</label>
            @*@Html.DropDownListFor(x => x.CurrentConfig.Operator, Model.CurrentConfig.OperatorList, new { @id = "operator-input", @class = "form-control" })*@
        </div>
        <div class="form-group">
            <label for="attribute-input">Filter by attribute:</label>
            @*@Html.DropDownListFor(x => x.CurrentConfig.Operator, Model.CurrentConfig.OperatorList, new { @id = "operator-input", @class = "form-control" })*@
        </div>
        <button type="submit" class="btn btn-default">Refresh</button>
    @*}*@
</div>
<div class="row">
    <canvas id="myChart"></canvas>
</div>

@section scripts{
    <script type="text/javascript" language="javascript">
            var miki = '@User.Identity.Name';
            $(".inner").append(miki);
            var ctx = document.getElementById('myChart').getContext('2d');
            var myChart = new Chart(ctx, {
                type: 'line',
                data: {
                    labels: ['M', 'T', 'W', 'T', 'F', 'S', 'S'],
                    datasets: [{
                        label: miki,
                        data: [12, 19, 3, 17, 6, 3, 7],
                        backgroundColor: "rgba(153,255,51,0.4)"
                    }, {
                        label: 'oranges',
                        data: [2, 29, 5, 5, 2, 3, 10],
                        backgroundColor: "rgba(255,153,0,0.4)"
                    }]
                }
            });    </script>
}

